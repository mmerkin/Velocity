# Association

```bash
awk 'NR > 1 {
    id = $1
    goid = "GO:" $3
    if (id in go) {
        go[id] = go[id] ";" goid
    } else {
        go[id] = goid
    }
}
END {
    for (id in go) {
        print id "\t" go[id]
    }
}' GO.out > ${tag}_GO_association.txt
```
